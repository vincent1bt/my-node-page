const ShowComponent = require('./show');
const EditComponent = require('./edit');
const NewComponent = require('./new');
const IndexComponent = require('./index');

module.exports = {
  ShowComponent,
  EditComponent,
  IndexComponent,
  NewComponent,
}
